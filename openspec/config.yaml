# openspec/config.yaml
schema: spec-driven

context: |
  Tech stack: React 19, Vite 6, TypeScript, Tailwind 4.
  Architecture: Local-First (Dexie, Yjs, IndexedDB), PWA.
  Testing: Vitest, Biome (linting/formatting).
  Style: Premium Dark Mode, Blue LTI accents.

rules:
  proposal:
    - Include rollback plan for risky changes
    - Identify affected modules (Academic, Aether, Nexus)
  specs:
    - Use Given/When/Then format for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group tasks by phase (infrastructure, implementation, testing)
    - Use hierarchical numbering (1.1, 1.2, etc.)
  apply:
    - Follow existing code patterns (Local-First, CRDT)
    - Ensure offline-ready compatibility
  verify:
    - Run `npm run test` and `npm run lint`
    - Compare implementation against every spec scenario
  archive:
    - Warn before merging destructive deltas
